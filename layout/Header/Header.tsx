import HeaderProps from './Header.props';

const Header: React.FC<HeaderProps> = (props) => {
    return (
        <header {...props}>
            Header
        </header>
    );
};

export default Header;