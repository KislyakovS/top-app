import FooterProps from './Footer.props';

const Footer: React.FC<FooterProps> = (props) => {
    return (
        <footer {...props}>
            Footer
        </footer>
    );
};

export default Footer;